public abstract class ComponentStateProcessor
{
    protected TimeControlSettings Settings;

    public ComponentStateProcessor(TimeControlSettings settings)
    {
        Settings = settings;
    }
}
